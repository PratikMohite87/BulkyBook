@model Category

<form method="post">
    <div class="border p-2 mt-4">

        <div class="row pb-2">
            <h2 class="text-primary">Create Category</h2>
            <hr />
        </div>

        @*<div asp-validation-summary=All></div>*@      @*Gives all validation as summary at top*@

        <div class="mb-3">
            <label asp-for=Name></label>
            <input asp-for=Name class="form-control"/>
            <span asp-validation-for=Name class="text-danger"></span>   @*Shows message of validation, part of server side validation*@
        </div>

        <div class="mb-3">
            <label asp-for=DisplayOrder></label>
            <input asp-for=DisplayOrder class="form-control"/>
            <span asp-validation-for=DisplayOrder class="text-danger"></span>    @*Shows message of validation, part of server side validation*@
        </div>

        <button type="submit" class="btn btn-primary" style="width:150px">Create</button>
        <a asp-controller="Category" asp-action=Index class="btn btn-secondary" style="width:150px">Back to List</a>
    </div>
</form>

@section Scripts
{
    @{
        <partial name="_ValidationScriptsPartial"></partial>        /*Enables Clientside validation, all above validation tags which were part of server side validation now willalso be a part of client side validation*/
    }
}